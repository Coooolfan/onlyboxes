> [!WARNING]
> Breaking change: `computerUse` now requires `worker_id` to dispatch, and `readImage`/`exportFile` select a Worker System via `session_id="CU:<worker_id>"`; the old value `computerUse` is treated as a sandbox session ID.

## feat
* Support multiple `worker-sys` workers per account with explicit `worker_id` / `CU:<worker_id>` routing https://github.com/Coooolfan/onlyboxes/commit/a186359

## fix
* Reject migration 00009 rollback while any account owns multiple `worker-sys` workers https://github.com/Coooolfan/onlyboxes/commit/2d9a12d

Full Changelog: https://github.com/Coooolfan/onlyboxes/compare/0.11.0...0.12.0-beta.1
