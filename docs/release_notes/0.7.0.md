## feat
* Add LobeHub sandbox runtime image and publish three variants (`default`, `default-cn`, `lobehub`) under `coolfan1024/onlyboxes-runtime` https://github.com/Coooolfan/onlyboxes/pull/7
* Add Sandbox Metadata API at `GET /api/v1/sandbox/metadata` exposing limits, capability availability, and worker summary https://github.com/Coooolfan/onlyboxes/pull/7
* Forward presigned upload headers (`x-amz-*`, `Content-Type`, `Content-MD5`) from MCP export-file through terminalResource workers https://github.com/Coooolfan/onlyboxes/pull/7
* Expose `WORKER_CONSOLE_INSECURE` in worker-sys Temporary Probe and hide inert fields https://github.com/Coooolfan/onlyboxes/commit/9765221
* `install.py` and `worker-startup.sh` now resolve the latest release tag from GitHub at runtime, with a fallback tag for offline or rate-limited environments https://github.com/Coooolfan/onlyboxes/commit/e3b4963

## fix
* Filter terminalResource upload headers through an allowlist to prevent overrides of reserved or hop-by-hop headers https://github.com/Coooolfan/onlyboxes/pull/7
* Keep capability metadata snapshot consistent across `onlineNodes` and `maxInflight` computation https://github.com/Coooolfan/onlyboxes/pull/7

## ci
* Switch package-release workflow to tag-driven (`push: tags '*.*.*'`) with prerelease auto-detection, draft → upload → publish staging, build-website job, and Cloudflare Workers deploy https://github.com/Coooolfan/onlyboxes/commit/e3b4963

## chore / docs
* Drop the release-defaults checklist; web and website default tags are now injected at build time via `VITE_CONSOLE_VERSION` / `VITE_DEFAULT_TAG` https://github.com/Coooolfan/onlyboxes/commit/e3b4963

Full Changelog: https://github.com/Coooolfan/onlyboxes/compare/0.6.1...0.7.0
